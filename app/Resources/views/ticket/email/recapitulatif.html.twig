<!doctype html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport"
              content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Document</title>
    </head>
    <body>
        <h3>Mail de confirmation de paiement</h3>
        <br/>
        <p>Référence de paiement : {{ order.orderToken }}</p>
        <br/>
        <p>Vous venez de passer commande pour {{ order.nbTickets }} billets au musée du Louvre pour une visite le {{ order.visitDate|date('d/m/Y') }}</p>

        {% for ticket in tickets %}
            <div class="well">
                <p>Nom : {{ ticket.name }}</p>
                <p>Prénom : {{ ticket.lastName }}</p>
                <p>age : {{ ticket.age|age }}</p>
                <p>Tarif : {{ ticket.rate }}</p>
                <p>Prix : {{ ticket.price }} €</p>
            </div>
        {% endfor %}
    </body>
</html>