<div class="well">
    <div class="form-group">

        {# Génération du label #}
        {{ form_label(ticket.name, "Prénom : ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(ticket.name) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(ticket.name) }}
        </div>
    </div>

    <div class="form-group">

        {# Génération du label #}
        {{ form_label(ticket.lastName, "Nom : ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(ticket.lastName) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(ticket.lastName) }}
        </div>
    </div>

    <div class="form-group">

        {# Génération du label #}
        {{ form_label(ticket.age, "Date de naissance : ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(ticket.age) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(ticket.age) }}
        </div>
    </div>

    <div class="form-group">

        {# Génération du label #}
        {{ form_label(ticket.country, "Pays : ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(ticket.country) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(ticket.country) }}
        </div>
    </div>

    <div class="form-group">

        {# Génération du label #}
        {{ form_label(ticket.reduced_price, "Tarif réduit : ", {'label_attr': {'class': 'col-sm-2 control-label'}}) }}

        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(ticket.reduced_price) }}

        <div class="col-sm-10">
            {# Génération de l'input. #}
            {{ form_widget(ticket.reduced_price) }}
        </div>
    </div>

</div>